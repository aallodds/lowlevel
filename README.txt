'lowlevel' contains various test programs to check for super lowlevel things
on the target machine.

prefixed with "lol" since it stands for "low-level"

(lo)-(l)evel

right now, you need a compiler that has C99.

just chmod +x the 'compile.sh' script and then compile it
and run the programs

./bin/lol_endian
./bin/lol_datamodel